export enum Route {
  HOME = "/",
  PANEL = "/panel",
  DASHBOARD = "/dashboard",
  SETTINGS = "/settings",
}

export enum RouteLabel {
  HOME = "Home",
  DASHBOARD = "Dashboard",
  SETTINGS = "Settings",
}

export enum RouteGroup {
  MAIN = "main",
  SIDEBAR = "sidebar",
  USER = "user",
}
